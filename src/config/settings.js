/**
 * Настройки UI и анимации.
 */

export const ROULETTE_SCROLL_DURATION_MS = 10000;

/** Показывать обратный отсчёт (3, 2, 1) перед открытием. false — открывать сразу */
export const COUNTDOWN_ENABLED = true;

/** Бонус за первое открытие кейса за день (добавляется к балансу) */
export const DAILY_BONUS_BALANCE = 50;
